// App.tsx is no longer the main entry point.
// The app is structured as: main.tsx → RouterProvider → AppLayout → Pages
// This file is kept for backwards compatibility but can be safely deleted.

export default function App() {
    return null;
}
